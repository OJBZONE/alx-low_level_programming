#include<stdio.h>
/**
 * main -is the entry point of the programi
 *
 * Return zero if no error, Non-zero value if errors
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
